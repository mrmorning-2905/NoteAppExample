<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.NoteApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
         <item name="colorPrimary">@color/app_primary_color</item>
    </style>

    <style name="Theme.NoteApp" parent="Base.Theme.NoteApp" >
        <item name="android:statusBarColor">@color/app_primary_color</item>
        <item name="android:navigationBarColor">@color/app_primary_color</item>
    </style>
    <style name="SplashTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_background</item>
        <item name="android:statusBarColor">@color/app_primary_color</item>
        <item name="android:navigationBarColor">@color/app_primary_color</item>
    </style>

    <style name="FloatingBtn" parent="Widget.Material3.FloatingActionButton.Primary" >
        <item name="colorPrimary">@color/app_primary_color</item>
    </style>
</resources>